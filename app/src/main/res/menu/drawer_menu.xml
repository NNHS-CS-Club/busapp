<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <item android:title="School">
        <menu>
            <group
                android:id="@+id/menu_top"
                android:checkableBehavior="single">
                <item
                    android:id="@+id/nav_user_bus"
                    android:icon="@drawable/ic_user_bus"
                    android:title="User Bus" />
                <item
                    android:id="@+id/nav_all_buses"
                    android:icon="@drawable/ic_all_buses"
                    android:title="@string/drawer_all_buses" />
                <item
                    android:id="@+id/nav_settings"
                    android:icon="@drawable/ic_settings"
                    android:title="@string/drawer_settings" />
            </group>

            <!-- Creates a Logout Option
            <group
            android:id="@+id/menu_bottom"
            android:checkableBehavior="single">
            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/ic_logout"
                android:title="@string/drawer_logout" />
            </group>
            -->

        </menu>
    </item>
</menu>